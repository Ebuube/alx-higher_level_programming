#!/usr/bin/node
// A rectangle class with a constructor that validates type of arguments

class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
}

module.exports = Rectangle;
