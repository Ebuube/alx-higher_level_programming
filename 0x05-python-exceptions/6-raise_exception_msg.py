#!/usr/bin/python3


def raise_exception_msg(message=""):
    """A function that raises a Name exception with a message

    Args:
        message: a string to be displayed on error message
    Returns:
        None
    """
    raise NameError(message)
